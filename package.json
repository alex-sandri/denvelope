{
  "name": "denvelope",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "start": "firebase serve --only hosting --host 0.0.0.0",
    "build": "npm run lint && webpack --mode production && node src/scripts/compile-html.js && webpack --mode production --config webpack.sw.config.js",
    "build:clean:windows": "cd public/assets && del css /s /q && del js /s /q && cd ../../ && npm run build",
    "build:ci": "npm run lint && mkdir public/assets/{css,js} && webpack --mode production && node src/scripts/compile-html.js && webpack --mode production --config webpack.sw.config.js && rm public/assets/css/style-bundle.js",
    "test": "npm run lint:test && set FIRESTORE_EMULATOR_HOST=localhost:5002 && firebase emulators:exec --only firestore \"jest --detectOpenHandles\"",
    "lint": "npx eslint src/ts/**/*.ts",
    "lint:fix": "npx eslint src/ts/**/*.ts --fix",
    "lint:test": "npx eslint test/**/*.ts --plugin jest --env jest --rule guard-for-in:0 --rule no-redeclare:0 --rule no-return-await:0 --rule no-empty:0",
    "deploy:functions": "firebase deploy --only functions"
  },
  "author": "Alex Sandri <me@alexsandri.com> (https://alexsandri.com)",
  "license": "UNLICENSED",
  "devDependencies": {
    "@firebase/testing": "^0.20.5",
    "@types/jest": "^26.0.3",
    "@types/stripe-v3": "^3.1.19",
    "@typescript-eslint/eslint-plugin": "^3.5.0",
    "@typescript-eslint/parser": "^3.4.0",
    "css-loader": "^3.6.0",
    "eslint": "^7.3.1",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-import-resolver-webpack": "^0.12.2",
    "eslint-plugin-import": "^2.21.2",
    "eslint-plugin-jest": "^23.17.1",
    "extract-loader": "^5.1.0",
    "file-loader": "^6.0.0",
    "fork-ts-checker-webpack-plugin": "^5.0.5",
    "glob": "^7.1.6",
    "handlebars": "^4.7.6",
    "jest": "^26.1.0",
    "jest-environment-uint8array": "^1.0.0",
    "node-sass": "^4.14.1",
    "sass-loader": "^8.0.2",
    "ts-jest": "^26.1.1",
    "ts-loader": "^7.0.5",
    "typescript": "^3.9.5",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "firebase": "^7.15.5",
    "firebaseui": "^4.5.1",
    "monaco-editor": "^0.20.0"
  }
}
